{
  "name": "atlas-mcp-greeter",
  "version": "0.1.0",
  "type": "module",
  "description": "TypeScript MCP server for Atlas (proves TS + language-agnostic tool layer).",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
